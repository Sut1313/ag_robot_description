# ROS 2 / colcon 构建产物
build/
install/
log/

# 编辑器/系统杂项
*.pyc
__pycache__/
.vscode/
.idea/
*~

# 本机历史备份
*.bak
*.bak2
*.bak_*
*.orig

# meshes/*.obj + ag_robot.mtl 是 SolidWorks 导出的原始逐部件配色数据(约 99 MB),
# 当前 URDF 不引用它们(见 README「为什么车体用 .stl」), 为避免仓库过大不进版本库。
# 需要做"按部件色拆分网格"时再本地保留即可。
meshes/*.obj
meshes/*.mtl

# lidar_capture_png.py 的默认输出
vlp16_scan.png
